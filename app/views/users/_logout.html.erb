<% if user_signed_in? %>
  <li>
  <%= link_to('Logout', destroy_user_session_path, method: :delete, :class => "button btn btn-danger btn-xs") %>        
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path, :class => "button btn btn-danger")  %>  
  </li>
<% end %>