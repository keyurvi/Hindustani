<h1>Listing Users</h1>
 
<table>
  <tr>
    <th>Name</th>
  </tr>
 
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user_path(user) %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'new user', new_user_path %>


<%= will_paginate @users %>